#!/usr/bin/python3


def multiple_returns(sentence):
    res = tuple(sentence)

    if len(sentence) == 0:
        sentence[0] = none
    else:
        first_char = sentence[0]

    return (len(sentence), first_char)
